/**
 * Metadata information (title, description, etc..) for the pages of the application.
 */
export interface IPageMetadata {
    /**
     * Title of the page.
     */
    title: string;
    /**
     * Description of the page.
     */
    description: string;
    /**
     * Uril of the image displayed when sharing the page.
     */
    image: string;
}
